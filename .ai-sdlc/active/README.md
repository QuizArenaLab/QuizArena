# Active Workspace

This workspace represents the ONLY active engineering sprint.

These files are overwritten for every sprint. They are never copied into permanent sprint folders. Git history becomes the permanent sprint record.
