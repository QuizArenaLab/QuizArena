export interface DateCapabilities {
  range: boolean;
  multiple: boolean;
  presets: boolean;
  timezone: boolean;
  duration: boolean;
  validation: boolean;
  keyboardNavigation: boolean;
  accessibility: boolean;
}
