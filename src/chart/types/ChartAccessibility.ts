/**
 * A dedicated structure for Accessibility configuration inside Charts.
 */
export interface ChartAccessibility {
  ariaLabel?: string;
  ariaDescription?: string;
  longDescription?: string;
  keyboardNavigation?: boolean;
}
